<div class="fields">
    <div class="min_attraction">
    	<!-- x icon to remove -->
      	<%= f.link_to_remove("X") %><br><br>
      	<!-- fields -->
    	<%= f.input :student_id, collection: Student.active.alphabetical, prompt: "Select student..."%>
    	<%= f.input :payment_status %>
    	<%= f.input :points_earned %>
	</div>
</div>